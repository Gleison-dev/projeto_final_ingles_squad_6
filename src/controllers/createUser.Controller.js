function createPUser(id,name, age, gender, email, password) {

    if(!name || !age || !gender || !email || !password){
            console.log("Preencha todos os campos!!")
            return
    }
}